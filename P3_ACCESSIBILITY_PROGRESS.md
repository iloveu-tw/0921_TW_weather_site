# P3 無障礙與色覺友善進度紀錄

> 暫停日期：2026-09-23  
> 目前分支：`codex/p3-accessibility`  
> 狀態：P3 與分頁／排序修正完成，等待使用者 localhost 手動驗證

## 已完成但尚未 Commit 的 P3 修改

- 次要文字色彩由 `#64748b` 調整為 `#8291a7`，深色卡片對比約由 `3.75:1` 提升至 `5.57:1`。
- 零雨量測站改用空心標記，無資料測站改用虛線標記。
- 圖例加入邊框；零雨量使用空心樣式，無資料使用斜紋樣式。
- 搜尋與縣市欄位補回可見的 `:focus-visible` 外框。
- 地圖底圖、氣象模式及圖層控制加入群組名稱與明確 ARIA 狀態。
- Leaflet 縮放、Popup 與關閉控制加入中文無障礙名稱。
- 表格排序改用明確的升冪／降冪箭頭。
- 零雨量 Popup 數值改用高對比文字。
- 數據清單分頁加入「第一頁／最後一頁」，並補齊按鈕狀態與無障礙名稱。

## 已完成驗證

- `npm test`：8／8 通過。
- `npm run lint`：通過。
- `npx tsc --noEmit`：通過。
- `npm run build`：通過。
- 1440px 畫面檢視正常，無水平溢出。
- 自動瀏覽器檢查：鍵盤 Focus、搜尋至縣市的 Tab 順序、ARIA 群組、圖例樣式、排序方向及 Popup 語意通過。

## 已修正議題：分頁看似無反應、排序看似只作用於目前頁

### 使用者觀察

- 數據清單的「上一頁／下一頁」按鈕沒有反應。
- 點擊欄位排序後，看起來只排序目前頁面。

### 已確認根因

`components/WeatherTable.tsx` 的已選測站 Effect 同時依賴：

- `currentPage`
- `selectedStation`
- `sortedStations`

只要存在已選測站，手動換頁或重新排序後，Effect 就會再次計算該測站所在頁，並執行 `setCurrentPage(selectedPage)`。因此頁碼會立刻被拉回已選測站所在頁，造成分頁按鈕沒有反應，以及排序只影響目前頁的錯覺。

`lib/weather-view.ts` 已確認是先對完整篩選結果排序，再執行 `slice` 分頁；排序演算法本身是全資料排序，不是單頁排序。

### 已完成修正

1. 已選測站只在「新測站被選取」時自動切換到該測站所在頁。
2. 使用者手動換頁或點擊排序後，不再被舊的已選測站拉回原頁。
3. 已將「切換至測站頁面」與「捲動至選取列」拆成兩個 Effect。
4. 選取列使用更明顯的背景、青色上下邊框、左側標線及高對比文字。
5. 選取列會置於捲動容器中央；頁首／頁尾資料不足時移至可允許的最接近中央位置。
6. 保留地圖點選測站後，自動找到清單頁面與選取列的既有功能。

### 自動瀏覽器回歸證據

- 選取一般測站列後，列中心與容器中心誤差為 1px。
- 選取接近頁尾的測站列後，捲動位置到達容器最大值，符合最接近中央的限制。
- 選取測站後按下一頁，可維持於第 2 頁，不再回彈。
- 測站名稱升冪排序後，第 1 頁末筆「八甲」與第 2 頁首筆「八里」順序連續，確認為全資料排序後再分頁。

## 尚未執行

- P3 修改尚未 Commit、Push 或建立 PR。
- 尚未更新正式網站。
- 兩份原始建議文件維持未追蹤且未修改。

## 下一步

1. 使用者於 localhost 手動確認 P3、分頁、排序與選取列置中。
2. 確認後才 Commit、Push、建立 Preview PR。
3. Preview 通過並取得明確授權後，才合併 `main`。
